import React, { useState } from 'react';

export default function AddProduct(props) {

    // const [Products,setProducts] = useState()
    // const [flag,setflag] = useState(false)



    
    return (
        <div>
            {/* {Products.map((element,index) => <button style={{backgroundColor:element.mode}} onClick={()=>setProducts(!flag)} key={ 'product-' + index}>{element.name}</button>)} */}
        </div>
    )
}

